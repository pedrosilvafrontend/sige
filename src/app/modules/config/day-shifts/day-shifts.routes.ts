import { Route } from '@angular/router';
import { DayShiftsComponent } from './day-shifts.component';

export const DAY_SHIFTS_ROUTE: Route[] = [
  {
    path: '',
    component: DayShiftsComponent,
  },

];
