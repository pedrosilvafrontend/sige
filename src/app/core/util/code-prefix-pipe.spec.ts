import { CodePrefixPipe } from './code-prefix-pipe';

describe('CodePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new CodePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
