import { ColoringByPipe } from './coloring-by-pipe';

describe('ColoringByPipe', () => {
  it('create an instance', () => {
    const pipe = new ColoringByPipe();
    expect(pipe).toBeTruthy();
  });
});
