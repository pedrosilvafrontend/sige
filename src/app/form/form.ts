import { UserForm } from './user.form';
import { ClassForm } from './class.form';
import { SchoolForm } from './school.form';

export class Form {
  static user = UserForm;
  static schoolClass = ClassForm;
  static school = SchoolForm
}

