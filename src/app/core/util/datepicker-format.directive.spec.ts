import { DatepickerFormatDirective } from './datepicker-format.directive';

describe('DatepickerFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerFormatDirective();
    expect(directive).toBeTruthy();
  });
});
